export const selectIsAuthenticated = (state) => state.auth.isAuthenticated;
export const selectUser = (state) => state.user.user;
export const selectLoginError = (state) => state.auth.error;

/*fichier selectooooor */
